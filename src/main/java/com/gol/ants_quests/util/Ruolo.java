package com.gol.ants_quests.util;

public enum Ruolo {
    guest,
    studente,
    admin

    /*
     * GUEST("guest"),
     * STUDENTE("studente"),
     * ADMIN("admin");
     * 
     * private String value;
     * 
     * Ruolo(String value) {
     * this.value = value;
     * }
     * 
     * public String getValue() {
     * return value;
     * }
     * 
     * public static Ruolo fromValue(String value) {
     * for (Ruolo ruolo : Ruolo.values()) {
     * if (ruolo.value.equalsIgnoreCase(value)) {
     * return ruolo;
     * }
     * }
     * throw new IllegalArgumentException("Unknown enum value: " + value);
     * }
     */
}